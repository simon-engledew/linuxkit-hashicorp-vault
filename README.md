Run Hashicorp Vault on LinuxKit

Creates a standalone appliance running Hashicorp Vault.

On macOS, with [LinuxKit](https://github.com/linuxkit/linuxkit), VMWare Fusion and Docker installed:

`make vmware`
